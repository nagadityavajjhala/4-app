{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "callSignals": {
      "$calleeId": {
        "$conversationId": {
          ".read": "auth != null",
          ".write": "auth != null"
        }
      }
    }
  }
}
